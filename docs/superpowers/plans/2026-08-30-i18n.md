# 多言語化（i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** UI を 8 言語（ja / en / de / es / fr / ko / pt-BR / zh-TW）に対応させ、Rust のメッセージを英語に統一し、Chrome に注入するオーバーレイ文言をフロントの翻訳で差し替えられるようにする。

**Architecture:** `vue-i18n@11`（`legacy: false`）を `src/i18n/index.ts` で初期化し、`src/locales/*.json`（YTDown 式のネストキー、117 キー）を読む。コンポーネントは `useI18n()` の `t()` を使う。既定言語は `localStorage` → `navigator.language` → `en`。Rust は翻訳表を持たず、オーバーレイ文言は `capture_screenshots` の `overlay` 引数で受け取る。Rust のユーザー向け文言は英語に統一する。

**Tech Stack:** vue-i18n 11 / Vue 3.5 + TS / Rust（serde）

**Spec:** `docs/superpowers/specs/2026-08-30-i18n-design.md`

## Global Constraints

- 新規依存は `vue-i18n@^11` のみ
- 8 ロケールファイルのキー集合と `{placeholder}` 集合は完全一致（`scripts/check-locales.mjs` が `pnpm test` の先頭で検証）
- テンプレートで `$t` は使わない。各コンポーネントで `const { t } = useI18n()`
- `ja.json` / `en.json` は `.superpowers/sdd/2026-08-30-i18n/seed/`（計画実行時に scratchpad からコピー）の草案を正とし、キーの追加・改名は `keymap.md` を更新してから行う
- Rust の非テストコードにユーザー向け日本語文字列を残さない（テスト名・コメントは対象外）。オーバーレイ文言は Rust にハードコードしない
- 既存の挙動（撮影・合成・取り込み）は変えない。`pnpm test` 合格、`cargo check` 警告 0
- コミットメッセージは `<type>(scope): <emoji> 日本語`。`.superpowers/`、`dist/`、`dist-release/` はコミットしない

## File Structure

| パス | 責務 |
|---|---|
| `src/i18n/index.ts` | `SUPPORTED_LOCALES`, `LOCALE_NAMES`, `detectLocale`, `setLocale`, `i18n` インスタンス |
| `src/locales/{ja,en,de,es,fr,ko,pt-BR,zh-TW}.json` | 翻訳（117 キー） |
| `scripts/check-locales.mjs` | キー / プレースホルダの整合検証 |
| `src/main.ts` | `app.use(i18n)` |
| `src/App.vue` | `t()` 化、言語セレクト、`overlay` 引数の組み立て |
| `src/components/DeviceFramePanel.vue` | `t()` 化 |
| `src/types/frames.ts` | `OverlayLabels` 型 |
| `src-tauri/src/main.rs` | `OverlayLabels` 受け取りと注入 JS への埋め込み、英語メッセージ |
| `src-tauri/src/frames/*.rs` | 英語メッセージ + テスト更新 |
| `README.md`, `AGENTS.md` | Languages 節、制約 |

---

### Task 1: vue-i18n の導入・ロケール草案・整合チェック・言語セレクト

**Files:**
- Modify: `package.json`（`vue-i18n`、`test` script）
- Create: `src/i18n/index.ts`, `src/locales/ja.json`, `src/locales/en.json`, `scripts/check-locales.mjs`
- Modify: `src/main.ts`, `src/App.vue`（ヘッダーに言語セレクトのみ。他の文字列はまだ触らない）

**Interfaces:**
- Produces: `import { i18n, SUPPORTED_LOCALES, LOCALE_NAMES, setLocale, type SupportedLocale } from './i18n'`; `pnpm test` が `check-locales` を含む

- [ ] **Step 1: 依存追加と草案のコピー**

```bash
pnpm add vue-i18n@^11
mkdir -p src/locales
cp .superpowers/sdd/2026-08-30-i18n/seed/ja.json src/locales/ja.json
cp .superpowers/sdd/2026-08-30-i18n/seed/en.json src/locales/en.json
```

- [ ] **Step 2: `scripts/check-locales.mjs`**

```js
// 8 ロケールのキー集合と {placeholder} 集合が en.json と一致することを検証する
import { readFileSync, readdirSync } from 'node:fs'
import { join } from 'node:path'

const dir = new URL('../src/locales/', import.meta.url).pathname
const REQUIRED = ['ja', 'en', 'de', 'es', 'fr', 'ko', 'pt-BR', 'zh-TW']

const flatten = (obj, prefix = '', out = new Map()) => {
  for (const [k, v] of Object.entries(obj)) {
    const key = prefix ? `${prefix}.${k}` : k
    if (v && typeof v === 'object') flatten(v, key, out)
    else out.set(key, String(v))
  }
  return out
}
const placeholders = s => new Set([...s.matchAll(/\{(\w+)\}/g)].map(m => m[1]))

const files = readdirSync(dir).filter(f => f.endsWith('.json')).map(f => f.replace(/\.json$/, ''))
const missingFiles = REQUIRED.filter(l => !files.includes(l))
const en = flatten(JSON.parse(readFileSync(join(dir, 'en.json'), 'utf8')))
let errors = [...missingFiles.map(l => `missing locale file: ${l}.json`)]

for (const locale of files) {
  const m = flatten(JSON.parse(readFileSync(join(dir, `${locale}.json`), 'utf8')))
  for (const key of en.keys()) if (!m.has(key)) errors.push(`${locale}: missing key ${key}`)
  for (const [key, val] of m) {
    if (!en.has(key)) errors.push(`${locale}: extra key ${key}`)
    else {
      if (val.trim() === '') errors.push(`${locale}: empty value ${key}`)
      const a = [...placeholders(en.get(key))].sort().join(','), b = [...placeholders(val)].sort().join(',')
      if (a !== b) errors.push(`${locale}: placeholders differ for ${key} (en: ${a || '-'} / ${locale}: ${b || '-'})`)
    }
  }
}
if (errors.length) { console.error(errors.join('\n')); process.exit(1) }
console.log(`check-locales: ${files.length} locales, ${en.size} keys OK`)
```

`package.json` の `"test"` を `"node scripts/check-locales.mjs && pnpm run build && cd src-tauri && cargo test"` にする。この時点では de 等が無いので `check-locales` は失敗する（Task 6 まで `pnpm test` は失敗でよい。`pnpm run build` と `cargo test` を個別に使う）。

- [ ] **Step 3: `src/i18n/index.ts`**

```ts
import { createI18n } from 'vue-i18n'
import ja from '../locales/ja.json'
import en from '../locales/en.json'
import de from '../locales/de.json'
import es from '../locales/es.json'
import fr from '../locales/fr.json'
import ko from '../locales/ko.json'
import ptBR from '../locales/pt-BR.json'
import zhTW from '../locales/zh-TW.json'

export const SUPPORTED_LOCALES = ['ja', 'en', 'de', 'es', 'fr', 'ko', 'pt-BR', 'zh-TW'] as const
export type SupportedLocale = (typeof SUPPORTED_LOCALES)[number]

export const LOCALE_NAMES: Record<SupportedLocale, string> = {
  ja: '日本語',
  en: 'English',
  de: 'Deutsch',
  es: 'Español',
  fr: 'Français',
  ko: '한국어',
  'pt-BR': 'Português (Brasil)',
  'zh-TW': '繁體中文',
}

const STORAGE_KEY = 'responsiveshot_language'

const isSupported = (v: string | null | undefined): v is SupportedLocale =>
  !!v && (SUPPORTED_LOCALES as readonly string[]).includes(v)

/** localStorage → navigator.language（先頭一致）→ 'en' */
export const detectLocale = (): SupportedLocale => {
  try {
    const saved = localStorage.getItem(STORAGE_KEY)
    if (isSupported(saved)) return saved
  } catch {
    /* localStorage が使えない環境では無視 */
  }
  const nav = (navigator.language || '').toLowerCase()
  if (nav.startsWith('ja')) return 'ja'
  if (nav.startsWith('zh')) return 'zh-TW'
  if (nav.startsWith('pt')) return 'pt-BR'
  if (nav.startsWith('ko')) return 'ko'
  if (nav.startsWith('fr')) return 'fr'
  if (nav.startsWith('es')) return 'es'
  if (nav.startsWith('de')) return 'de'
  return 'en'
}

export const i18n = createI18n({
  legacy: false,
  locale: detectLocale(),
  fallbackLocale: 'en',
  messages: { ja, en, de, es, fr, ko, 'pt-BR': ptBR, 'zh-TW': zhTW },
})

export const setLocale = (locale: SupportedLocale) => {
  i18n.global.locale.value = locale
  try {
    localStorage.setItem(STORAGE_KEY, locale)
  } catch {
    /* ignore */
  }
  document.documentElement.lang = locale
}

document.documentElement.lang = i18n.global.locale.value
```

Task 6 までは de 等のファイルが無いので、Task 1 の時点では `de` … `zhTW` の import と `messages` の該当行を **一時的に `en` で代用**（`de: en, es: en, …`）し、Task 6 で本物に差し替える。JSON import には `tsconfig` の `resolveJsonModule: true` が必要（既定の Vue TS 設定に含まれているか確認し、無ければ `tsconfig.app.json` の `compilerOptions` に追加）。

- [ ] **Step 4: `src/main.ts`**

```ts
import { createApp } from 'vue'
import App from './App.vue'
import { i18n } from './i18n'
import './style.css'
import 'virtual:uno.css'   // 既存の import 群はそのまま

createApp(App).use(i18n).mount('#app')
```

（既存の import 行を維持しつつ `.use(i18n)` を足す。）

- [ ] **Step 5: ヘッダーに言語セレクト**

`App.vue` の `<script setup>` に:

```ts
import { useI18n } from 'vue-i18n'
import { LOCALE_NAMES, SUPPORTED_LOCALES, setLocale, type SupportedLocale } from './i18n'
const { t, locale } = useI18n()
const changeLocale = (e: Event) => setLocale((e.target as HTMLSelectElement).value as SupportedLocale)
```

テーマの `<select v-model="colorPreference">` の直前（同じ `absolute right-0 top-0` の div 内、`flex gap-2` にする）に:

```vue
          <select :value="locale" @change="changeLocale" :aria-label="t('language.label')" class="bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-sm rounded-lg px-2 py-1 text-gray-700 dark:text-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500">
            <option v-for="l in SUPPORTED_LOCALES" :key="l" :value="l">{{ LOCALE_NAMES[l] }}</option>
          </select>
```

- [ ] **Step 6: ビルドとコミット**

Run: `pnpm run build 2>&1 | tail -3`（エラーなし）、`node scripts/check-locales.mjs`（de 等が無いので失敗するのは想定どおり — 出力に `missing locale file` が 6 件だけ出ることを確認）

```bash
git add package.json pnpm-lock.yaml src/i18n/index.ts src/locales/ja.json src/locales/en.json scripts/check-locales.mjs src/main.ts src/App.vue tsconfig.app.json
git commit -m "feat(i18n): ✨ vue-i18n を導入し ja / en ロケールと言語切替セレクトを追加"
```

---

### Task 2: App.vue の全文字列を `t()` 化

**Files:**
- Modify: `src/App.vue`

**Interfaces:**
- Consumes: `keymap.md`（`.superpowers/sdd/2026-08-30-i18n/seed/keymap.md`）の App.vue 行（66 件）

- [ ] **Step 1: 置換**

`keymap.md` の App.vue の各行を、記載のキーで `t('…')` に置き換える。ルール:
- テンプレート内の文字列は `{{ t('key') }}`、属性は `:placeholder="t('key')"` / `:title="t('key')"`
- `ratioOptions` の `label` / `description` は配列を `computed` にして `t('widths.ratio.<id>.label')` を返す（`value` は既存の `'16:9'` 等のまま。`<id>` は `r16x9` 等）
- `statusMessage.value = "…${err}"` は `t('status.captureError', { error: String(err) })`
- `ratioPreview` の文言（`フルページでは無効` 等）も `t()`
- 手動操作オーバーレイの文言は Task 4 で扱う（この Task では `invoke` 引数を増やさない）

- [ ] **Step 2: 残存チェックとビルド**

Run: `grep -n -P '[\x{3040}-\x{30ff}\x{4e00}-\x{9fff}]' src/App.vue | grep -v "^\s*//"` → コメント以外に日本語が残っていない。`pnpm run build` 合格

- [ ] **Step 3: Commit**

```bash
git add src/App.vue
git commit -m "feat(i18n): ✨ App.vue の UI 文字列を t() 化"
```

---

### Task 3: DeviceFramePanel.vue の全文字列を `t()` 化

**Files:**
- Modify: `src/components/DeviceFramePanel.vue`

- [ ] **Step 1: 置換**

`keymap.md` の DeviceFramePanel.vue の行（33 件）を `t()` に置き換える。`vendorLabels` / `categoryLabels` / `stateLabels` は `computed` または関数化して `t('frames.vendor.apple')` 等を返す。`runImport` のメッセージは `t('frames.status.skipped', { count, reasons })` を先に組み立てて `t('frames.status.imported', { names, skipped, dir })` に渡す。`const { t } = useI18n()` を追加

- [ ] **Step 2: 残存チェックとビルド**

Run: `grep -n -P '[\x{3040}-\x{30ff}\x{4e00}-\x{9fff}]' src/components/DeviceFramePanel.vue | grep -v "^\s*//"` → コメント以外なし。`pnpm run build` 合格

- [ ] **Step 3: Commit**

```bash
git add src/components/DeviceFramePanel.vue
git commit -m "feat(i18n): ✨ DeviceFramePanel の UI 文字列を t() 化"
```

---

### Task 4: Chrome オーバーレイの文言をフロントから渡す

**Files:**
- Modify: `src/types/frames.ts`（`OverlayLabels`）
- Modify: `src/App.vue`（`overlay` 引数）
- Modify: `src-tauri/src/main.rs`（`OverlayLabels` 受け取り、注入 JS）

**Interfaces:**
- Produces: `capture_screenshots(..., overlay: OverlayLabels)`; TS `OverlayLabels { startGif, startPng, startButton, countdown, cancel, intro, ready, recording, saving, progress }`（すべて string、`{label}` `{seconds}` `{frames}` はテンプレートのまま渡す）

- [ ] **Step 1: TS 型と invoke 引数**

`src/types/frames.ts` に:

```ts
/** Chrome に注入する手動操作オーバーレイの文言（Rust は翻訳表を持たない）。{label} {seconds} {frames} はテンプレートのまま渡す */
export interface OverlayLabels {
  startGif: string
  startPng: string
  startButton: string
  countdown: string
  cancel: string
  intro: string
  ready: string
  recording: string
  saving: string
  progress: string
}
```

`App.vue` の `invoke('capture_screenshots', {...})` に:

```ts
      overlay: {
        startGif: t('overlay.startGif'), startPng: t('overlay.startPng'), startButton: t('overlay.startButton'),
        countdown: t('overlay.countdown'), cancel: t('overlay.cancel'), intro: t('overlay.intro'),
        ready: t('overlay.ready'), recording: t('overlay.recording'), saving: t('overlay.saving'),
        progress: t('overlay.progress'),
      } satisfies OverlayLabels,
```

（vue-i18n は `{label}` のような未指定プレースホルダを空にする可能性があるため、テンプレートのまま渡すキーは `t('overlay.startButton', { label: '{label}', seconds: '{seconds}' })` のように **プレースホルダ自身を値として渡す**。`recording` / `countdown` / `progress` も同様。）

- [ ] **Step 2: Rust 側**

`main.rs` に:

```rust
/// Chrome に注入する手動操作 UI の文言。フロントが翻訳して渡す（`{label}` `{seconds}` `{frames}` はテンプレート）
#[derive(serde::Deserialize, Clone)]
struct OverlayLabels {
    start_gif: String,
    start_png: String,
    start_button: String,
    countdown: String,
    cancel: String,
    intro: String,
    ready: String,
    recording: String,
    saving: String,
    progress: String,
}

/// JS のシングルクォート文字列リテラルに埋め込める形にする
fn js_str(s: &str) -> String {
    s.replace('\\', "\\\\").replace('\'', "\\'").replace('\n', "\\n").replace("</", "<\\/")
}
```

`capture_screenshots` の引数末尾に `overlay: OverlayLabels` を追加（Tauri は JS の `startGif` を `start_gif` に変換する）。注入 JS（`btn_text` の選択、`msg.innerHTML = '画面を操作して…'`、`btn.innerText = '🔴 {} ({}秒後に開始)'`、`cancelBtn.innerText = '中止'`、`msg.innerText = '操作可能になりました…'`、`録画中... 残り ${{total_duration}}秒`、`保存処理中...`、カウントダウン表示）を、`js_str(&overlay.xxx)` を `format!` に渡す形に置き換える。JS 側で秒数を埋める箇所は、テンプレートを JS 変数に入れて `.replace('{seconds}', String(n))` する（例: `const recordingTpl = '{}'; … msg.innerHTML = recordingTpl.replace('{seconds}', total_duration);`）。`start_button` は Rust で `.replace("{label}", &label)` してから JS の `.replace('{seconds}', …)` に任せる。GIF 進捗（`● 録画・保存中... 残り{}秒 ({}コマ完了)`）は Rust 側で `overlay.progress.replace("{seconds}", …).replace("{frames}", …)` してから `js_str` で埋め込む。

- [ ] **Step 3: 確認**

Run: `cd src-tauri && cargo build 2>&1 | grep -E "^error" ; cargo check 2>&1 | grep -c warning` → エラーなし・警告 0。`grep -n -P '[\x{3040}-\x{30ff}\x{4e00}-\x{9fff}]' src-tauri/src/main.rs` にオーバーレイ文言が残っていない（エラーメッセージの日本語は Task 5 で扱う）。`pnpm run build` 合格

- [ ] **Step 4: Commit**

```bash
git add src/types/frames.ts src/App.vue src-tauri/src/main.rs
git commit -m "feat(i18n): ✨ 手動操作オーバーレイの文言をフロントの翻訳から渡す"
```

---

### Task 5: Rust のユーザー向けメッセージを英語に統一

**Files:**
- Modify: `src-tauri/src/main.rs`, `src-tauri/src/frames/{catalog,compose,store,import,targets}.rs`（実装とテスト）

- [ ] **Step 1: 置換表**

| 現在 | 英語 |
|---|---|
| ユーザーによってキャプチャが中止されました | Capture cancelled by the user |
| Line {}: ユーザーによって中止されました | Cancelled by the user (line {}) |
| 保存先を作成できません {}: {} | Failed to create the frames directory {}: {} |
| 取り込み処理のスレッドが失敗しました: {} | Import task failed: {} |
| スクリーンショットの読み込みに失敗: {} | Failed to decode the screenshot: {} |
| PNG エンコードに失敗: {} | Failed to encode PNG: {} |
| フレーム画像の読み込みに失敗 {}: {} | Failed to load the frame image {}: {} |
| デバイスフレームは PNG 出力のみ対応しています | Device frames support PNG output only |
| フレームカタログが読み込まれていません | Frame catalog is not loaded |
| カタログに無いデバイスです: {} | Unknown device id: {} |
| カタログの読み込みに失敗: … | Failed to load the frame catalog: … |
| カタログ id が重複しています: {} | Duplicate catalog id: {} |
| カタログ id は英小文字・数字・ハイフンのみ: {:?} | Catalog id must be lowercase letters, digits and hyphens: {:?} |
| {}: vendor が不正です: {:?} | {}: invalid vendor: {:?} |
| {}: category が不正です: {:?} | {}: invalid category: {:?} |
| {}: screen が frame の外に出ています | {}: screen rect exceeds the frame |
| {}: pattern は {{variant}} を 1 回だけ含む必要があります | {}: pattern must contain {{variant}} exactly once |
| 背景色の形式が不正です: {:?}（#rrggbb 形式で指定してください） | Invalid background color: {:?} (use #rrggbb) |
| {} の色が選択されていません | No color variant selected for {} |
| フレームが見つかりません: {} ({})。取り込みをやり直してください | Frame not found: {} ({}). Import the frames again |
| フレーム画像を読めません {}: {}。取り込みをやり直してください | Cannot read the frame image {}: {}. Import the frames again |
| フレーム画像の寸法が不一致 (期待 {}x{}, 実際 {}x{}): {}。取り込みをやり直してください | Frame image size mismatch (expected {}x{}, got {}x{}): {}. Import the frames again |
| 寸法が不一致 (期待 {}x{}, 実際 {}x{}) | Size mismatch (expected {}x{}, got {}x{}) |
| 画像として読めません: {} | Not a readable image: {} |
| コピーに失敗 {} → {}: {} | Failed to copy {} → {}: {} |
| hdiutil を起動できません: {} | Failed to run hdiutil: {} |
| DMG のマウントに失敗: {}。Finder で既にマウント済みの場合は取り出してから再試行するか、「フォルダを取り込む」で /Volumes 内のボリュームを選んでください | Failed to mount the DMG: {}. If it is already mounted in Finder, eject it first, or use "Import folder" and pick the volume under /Volumes |
| 取り込み元が見つかりません: {} | Import source not found: {} |

上表に無い文言があれば同じ調子（名詞句 + コロン + 詳細、末尾ピリオドなし）で英訳する。

- [ ] **Step 2: テストの更新**

`contains("重複")` 等の日本語部分文字列アサーションを英語に更新（例: `"Duplicate catalog id"`, `"invalid vendor"`, `"invalid category"`, `"exceeds the frame"`, `"{variant}"`, `"Invalid background color"`, `"No color variant"`, `"Frame not found: iPhone 16 Pro (pink). Import the frames again"`, `"Size mismatch (expected 100x200, got 90x200)"`, `"Device frames support PNG output only"`, `"Frame not found"`, `"Unknown device id"`, `"Failed to mount the DMG"` / `"Failed to run hdiutil"`）。テスト名は変えない

- [ ] **Step 3: 確認**

Run: `cd src-tauri && cargo test 2>&1 | tail -3 && cargo check 2>&1 | grep -c warning`（全件合格、警告 0）。`for f in src-tauri/src/main.rs src-tauri/src/frames/*.rs; do awk '/#\[cfg\(test\)\]/{exit} {print FILENAME":"NR": "$0}' "$f"; done | grep -P '"[^"]*[\x{3040}-\x{30ff}\x{4e00}-\x{9fff}]' | grep -v '^\s*//'` → 文字列リテラル内の日本語が 0 件（コメントは除外）

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src
git commit -m "refactor(frames): ♻️ Rust のユーザー向けメッセージを英語に統一"
```

---

### Task 6: 残り 6 言語の翻訳と `check-locales` の合格

**Files:**
- Create: `src/locales/{de,es,fr,ko,pt-BR,zh-TW}.json`
- Modify: `src/i18n/index.ts`（Task 1 の一時 `en` 代用を本物の import に戻す）

- [ ] **Step 1: 翻訳**

`en.json` を元に 6 言語を作る。ルール: キーと `{placeholder}` は完全一致、`ResponsiveShot` と `#rrggbb` はそのまま、HTML（`<span …>●</span>`、`<br/>`）と絵文字は保持、専門語は spec §6 の統一表に従う。UI の短さを保つ（ボタンは 2〜3 語）。

- [ ] **Step 2: 検証**

Run: `node scripts/check-locales.mjs` → `check-locales: 8 locales, 117 keys OK`。`pnpm test` 全合格

- [ ] **Step 3: Commit**

```bash
git add src/locales src/i18n/index.ts
git commit -m "feat(i18n): ✨ de / es / fr / ko / pt-BR / zh-TW の翻訳を追加"
```

---

### Task 7: ドキュメント

**Files:**
- Modify: `README.md`（Languages / 対応言語 節、日英）、`AGENTS.md`（技術スタックと制約）

- [ ] **Step 1: README**

`## Features` に `- 8 UI languages (ja / en / de / es / fr / ko / pt-BR / zh-TW); switch from the header` を追加。`## Device frames` の前に節:

```markdown
## Languages / 対応言語

**English** — The UI is available in Japanese, English, German, Spanish, French, Korean, Portuguese (Brazil) and Traditional Chinese. The language follows your saved choice (header selector), otherwise the system language, otherwise English. Messages returned by the capture engine (errors, import results) are in English. Translations other than Japanese and English are machine-assisted; corrections are welcome.

**日本語** — UI は日本語 / 英語 / ドイツ語 / スペイン語 / フランス語 / 韓国語 / ポルトガル語（ブラジル）/ 繁体字中国語に対応しています。言語はヘッダーのセレクトで保存した選択 → システムの言語 → 英語 の順で決まります。キャプチャエンジンが返すメッセージ（エラー、取り込み結果）は英語です。日本語・英語以外の翻訳は機械翻訳ベースのため、修正歓迎です。
```

- [ ] **Step 2: AGENTS.md**

技術スタックに `- **i18n**: vue-i18n 11（legacy: false）。ロケールは src/locales/*.json、既定言語は localStorage → navigator.language → en` を追加。制約に `### 8. 多言語化` 節: UI 文字列は `t()` 必須・`$t` 不使用、8 ロケールのキー集合は `scripts/check-locales.mjs` で同期（`pnpm test` に含む）、Rust のユーザー向け文言は英語、オーバーレイ文言は `overlay` 引数で受け取る

- [ ] **Step 3: 最終確認とコミット**

Run: `pnpm test 2>&1 | tail -3`

```bash
git add README.md AGENTS.md
git commit -m "docs: 📖 多言語対応の説明（日英）と設計制約を追記"
```

- [ ] **Step 4: 手動 E2E（人が行う）**

1. 言語セレクトで各言語に切り替えると全セクションの文言が変わり、再起動後も保持される
2. 手動操作 ON で撮影 → Chrome のオーバーレイが選択言語で出る（ボタン、カウントダウン、録画中の残り秒数）
3. 背景色に `#zz` を入れて撮影 → 英語のエラー（`Invalid background color …`）が出る
4. `navigator.language` が ja の環境で localStorage を消して起動 → 日本語で起動
